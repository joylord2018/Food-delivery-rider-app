// API Configuration
// Controls whether to use real API calls or mock data

// Default to using mock data (false means use mock data)
export const API_CONFIG = {
  // Set to true to enable real API calls, false to use mock data
  useRealAPI: false,
  // Base URL for real API calls
  baseURL: '/api'
}
